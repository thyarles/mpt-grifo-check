{
  "devDependencies": {
    "eslint": "^10.9.1"
  },
  "name": "mpt-grifo-check",
  "version": "1.1.0",
  "private": true,
  "description": "Dev tooling for the MPT Grifo Check Chrome extension (not shipped)",
  "scripts": {
    "test": "node --test",
    "lint": "eslint ."
  }
}
